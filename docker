docker build -t <imageName:version> dockerFilePath
Ex: docker build -t tm-app:latest . 
. means inside this folder

update:
docker images -t tm-app:latest .

docker run -it tm-app:latest

docker ps

docker ps -a

docker run -it -p 9002:9005 tm-app:latest

docker images

